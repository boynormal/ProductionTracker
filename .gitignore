node_modules/
.next/
.env.local
.env
*.tsbuildinfo
next-env.d.ts

# รูปที่อัปโหลดจากผู้ใช้ (คงโฟลเดอร์ด้วย .gitkeep)
public/uploads/machines/*
!public/uploads/machines/.gitkeep

# เอกสารภายในเครื่อง / งานนำเสนอ (ไม่ติดตามใน git)
Documents/
docs/
Docs/

# Local DB dumps (never commit — may contain secrets / PII)
backups/*.dump
backups/*.sql
!backups/.gitkeep
